<%= form_with(model: face_compare, local: true) do |form| %>
  <% if face_compare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(face_compare.errors.count, "error") %> prohibited this face_compare from being saved:</h2>

      <ul>
      <% face_compare.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :profile_id %>
    <%= form.select :profile_id, options_for_select(Profile.all.map{ |s| [s.first_name, s.id] }) %>
  </div>

  <div class="field">
    <%= form.label :photo_compare %>
    <%= form.file_field :photo_compare, id: :face_compare_photo_compare %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
